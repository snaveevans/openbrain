# Open Brain Monorepo

This repo contains the Supabase backend plus the npm MCP package.

## Packages

- `packages/openbrain-mcp` - npm MCP server package published as `@snaveevans/openbrain-mcp`

## Build

```bash
npm install
npm run build
```

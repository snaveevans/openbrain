---
description: Address comments, reviews, and failing CI on a PR
argument-hint: "[PR number or URL — omit to use the PR for the current branch]"
disable-model-invocation: true
---

Use the `pr-respond` skill to address the feedback on $ARGUMENTS.

If no argument was given, find the open PR whose head branch is the
current branch.

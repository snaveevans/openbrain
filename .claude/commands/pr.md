---
description: Open a PR with issue link and repo template filled in
---

Use the `pr` skill. Prefer `validation-gate` if the user wants the full
pre-PR gate (rebase, review, risk, evidence).

User input: $ARGUMENTS
